apiVersion: v1
kind: Service
metadata:
  name: {{ template "1m-consumer.fullname" . }}
  labels:
    app: {{ template "1m-consumer.name" . }}
    chart: {{ template "1m-consumer.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "1m-consumer.name" . }}
    release: {{ .Release.Name }}
